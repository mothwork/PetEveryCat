mixin validationErrorSummary (errors)
    if errors 
        .error-class 
            p The following errors occured: 
            ul 
                each error in errors 
                    li= error 

mixin field(labelText, fieldName, fieldValue, fieldType = "text", placeholder = null)
    .form-group 
        label(for=fieldName)= labelText 
        if fieldType === "textarea"
            textarea(id=fieldName name=fieldName class="textarea")= fieldValue 
        else 
            input(type=fieldType id=fieldName name=fieldName value=fieldValue placeholder=placeholder class="form-input")
        